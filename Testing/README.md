Container Security Testing
Purpose

This testing workflow validates container security before application images are published to Docker Hub.

The goal is to identify known vulnerabilities during the CI/CD process and prevent insecure container images from progressing further in the deployment pipeline.